import { WidgetContainer } from "../lib/ui/layout"

export const Custom = () => {
  return (
    <WidgetContainer>
      Custom - ein frei konfigurierbare Liste mit Sensoren, Buttons, Eingabefelder die direkt per
      OPCUA an die SPS angebunden ist
    </WidgetContainer>
  )
}
